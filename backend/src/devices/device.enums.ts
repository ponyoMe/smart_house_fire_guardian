export enum DeviceType {
  SENSOR = 'sensor',
  ACTUATOR = 'actuator',
}

export enum DeviceStatus {
  NORMAL = 'normal',
  DETECTED = 'detected',
}

export enum DeviceState {
  ON = 'on',
  OFF = 'off',
  OPEN = 'open',
  CLOSED = 'closed',
}


